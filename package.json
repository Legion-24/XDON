{
  "name": "xcon-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "XCON (eXtensible Compact Object Notation) — schema-ambient structured data format. Monorepo for JS/TS and Python parsers.",
  "type": "module",
  "scripts": {
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.0.0",
    "prettier": "^3.2.5",
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^9.1.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
